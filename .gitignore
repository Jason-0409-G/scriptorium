__pycache__/
*.pyc
.DS_Store
# skill working outputs (don't commit generated libraries)
**/library.ris
**/library.bib
**/library.xlsx
**/library.csv
*-workspace/
.zotero_credentials
_opt/
# API credentials — never commit filled-in keys (keep only the template)
.env
*.env
!.env.example

